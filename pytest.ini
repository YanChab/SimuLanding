[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: expensive simulation-heavy tests excluded from local quick runs
    regression: non-regression and golden/reference curve tests